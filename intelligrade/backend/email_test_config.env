# 🧪 QUICK TEST EMAIL SETUP

# If you want to test immediately without setting up Gmail App Password,
# you can use a test email service:

# 1. OPTION: Use Mailtrap for testing (Free)
# SMTP_SERVER=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your_mailtrap_username
# SMTP_PASSWORD=your_mailtrap_password

# 2. OPTION: Use your personal email with app password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jusalyng@gmail.com
SMTP_PASSWORD=YOUR_16_CHARACTER_APP_PASSWORD_HERE

# After updating, restart server:
# cd backend && python -m backend.main

# Then test:
# curl http://localhost:8000/auth/email-status
# Should return: "configured": true